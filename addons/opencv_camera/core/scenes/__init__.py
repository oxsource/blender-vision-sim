"""Per-scene pure Python models (no ``bpy``).

Each module here holds the geometry, maths and file formats of one algorithm
scene, so it can be unit tested with a plain ``python3`` and reused by export
tools.  Blender integration lives in ``bl/scenes/``.
"""
